// This file contains placeholder examples of collection types,
// which are over-written during catalog build.

type ExampleSourceCollection = {
  hello: string;
};

type ExampleDerivedCollection = {
  world: string;
};
