# Tests

_Stub — deepen via `/platform-docs` as this concept is built out._

## Glossary

**Test**:
A fixture of source collection inputs and expected derivation outputs.

## Overview

A test verifies derivation behavior: it feeds fixture documents into source collections and asserts the outputs the derivation produces.

## Where this lives

- `crates/validation`, `crates/build` — test compilation and verification
