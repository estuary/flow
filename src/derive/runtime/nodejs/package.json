{
  "name": "estuary_runtime",
  "version": "0.1.0",
  "description": "Estuary Lambda runtime components for NodeJS",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "engines" : { "node" : ">=10.10" },
  "enginesStrict": true,
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "dependencies": {
    "stream-json": "^1.3"
  },
  "devDependencies": {
    "gts": "^2.0.0",
    "typescript": "~3.8.0",
    "@types/node": "^10.0.3",
    "@types/stream-json": "^1"
  }
}
