
/*
struct Executor {
    // Holds:
    // - handles to subordinate lambda processes.
    // - Map[source-collection]: target invocation URI OR sqlite function.
}

impl Executor {
    async fn begin() -> Transaction { Transaction{} }
}

struct Transaction {
    // Holds:
    // - Map[source-collection]: target stream OR sqlite context
    // - Aggregation state
}

impl Transaction {
    async fn input() {}
    async fn prepare() {}
    async fn commit() {}

    // Produces output stream into something?
}
*/
