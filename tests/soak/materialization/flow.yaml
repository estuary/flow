# Both materializations of `test/soak/accounts`, split into one file per task so
# each can be fed to `flowctl preview` on its own (see ../README.md
# "Running"): `views` needs the materialize-postgres image and a reachable
# PostgreSQL, while `ledger` is a Docker-free `local:` connector. This file groups
# them for a publish of both at once.
import:
  - views.flow.yaml
  - ledger.flow.yaml
