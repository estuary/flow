import:
  - transfers.flow.yaml

collections:
  acmeBank/last-large-send:
    schema: transfers.schema.yaml
    key: [/sender]

    derivation:
      typescript:
        module: last-large-send.flow.ts
      transform:
        fromTransfers:
          source:
            name: acmeBank/transfers
          publish:
            lambda: typescript
