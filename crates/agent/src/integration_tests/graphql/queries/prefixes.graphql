query Prefixes {
    prefixes(by: { minCapability: read }) {
        edges {
            cursor
            node {
                prefix
                userCapability
            }
        }
    }
}
