// This file is @generated by prost-build.
/// Generated client implementations.
#[cfg(feature = "shuffle_client")]
pub mod shuffle_client {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value
    )]
    use tonic::codegen::http::Uri;
    use tonic::codegen::*;
    /// Shuffle service provides coordinated, disk-backed shuffled reads.
    #[derive(Debug, Clone)]
    pub struct ShuffleClient<T> {
        inner: tonic::client::Grpc<T>,
    }
    impl ShuffleClient<tonic::transport::Channel> {
        /// Attempt to create a new client by connecting to a given endpoint.
        pub async fn connect<D>(dst: D) -> Result<Self, tonic::transport::Error>
        where
            D: TryInto<tonic::transport::Endpoint>,
            D::Error: Into<StdError>,
        {
            let conn = tonic::transport::Endpoint::new(dst)?.connect().await?;
            Ok(Self::new(conn))
        }
    }
    impl<T> ShuffleClient<T>
    where
        T: tonic::client::GrpcService<tonic::body::Body>,
        T::Error: Into<StdError>,
        T::ResponseBody: Body<Data = Bytes> + std::marker::Send + 'static,
        <T::ResponseBody as Body>::Error: Into<StdError> + std::marker::Send,
    {
        pub fn new(inner: T) -> Self {
            let inner = tonic::client::Grpc::new(inner);
            Self { inner }
        }
        pub fn with_origin(inner: T, origin: Uri) -> Self {
            let inner = tonic::client::Grpc::with_origin(inner, origin);
            Self { inner }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> ShuffleClient<InterceptedService<T, F>>
        where
            F: tonic::service::Interceptor,
            T::ResponseBody: Default,
            T: tonic::codegen::Service<
                    http::Request<tonic::body::Body>,
                    Response = http::Response<
                        <T as tonic::client::GrpcService<tonic::body::Body>>::ResponseBody,
                    >,
                >,
            <T as tonic::codegen::Service<http::Request<tonic::body::Body>>>::Error:
                Into<StdError> + std::marker::Send + std::marker::Sync,
        {
            ShuffleClient::new(InterceptedService::new(inner, interceptor))
        }
        /// Compress requests with the given encoding.
        ///
        /// This requires the server to support it otherwise it might respond with an
        /// error.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.send_compressed(encoding);
            self
        }
        /// Enable decompressing responses.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.accept_compressed(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_decoding_message_size(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_encoding_message_size(limit);
            self
        }
        /// Session is the top-level RPC opened by the Coordinator (typically
        /// shard-000). It manages the shuffle lifecycle, journal watches,
        /// and checkpoint aggregation.
        pub async fn session(
            &mut self,
            request: impl tonic::IntoStreamingRequest<Message = ::proto_flow::shuffle::SessionRequest>,
        ) -> std::result::Result<
            tonic::Response<tonic::codec::Streaming<::proto_flow::shuffle::SessionResponse>>,
            tonic::Status,
        > {
            self.inner.ready().await.map_err(|e| {
                tonic::Status::unknown(format!("Service was not ready: {}", e.into()))
            })?;
            let codec = tonic_prost::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static("/shuffle.Shuffle/Session");
            let mut req = request.into_streaming_request();
            req.extensions_mut()
                .insert(GrpcMethod::new("shuffle.Shuffle", "Session"));
            self.inner.streaming(req, path, codec).await
        }
        /// Slice is opened by the Session to each member. Each Slice reads
        /// an assigned subset of journals and routes documents to Queues.
        pub async fn slice(
            &mut self,
            request: impl tonic::IntoStreamingRequest<Message = ::proto_flow::shuffle::SliceRequest>,
        ) -> std::result::Result<
            tonic::Response<tonic::codec::Streaming<::proto_flow::shuffle::SliceResponse>>,
            tonic::Status,
        > {
            self.inner.ready().await.map_err(|e| {
                tonic::Status::unknown(format!("Service was not ready: {}", e.into()))
            })?;
            let codec = tonic_prost::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static("/shuffle.Shuffle/Slice");
            let mut req = request.into_streaming_request();
            req.extensions_mut()
                .insert(GrpcMethod::new("shuffle.Shuffle", "Slice"));
            self.inner.streaming(req, path, codec).await
        }
        /// Queue is opened by each Slice to each member. Documents flow
        /// from Slices through Queues to on-disk queue files.
        pub async fn queue(
            &mut self,
            request: impl tonic::IntoStreamingRequest<Message = ::proto_flow::shuffle::QueueRequest>,
        ) -> std::result::Result<
            tonic::Response<tonic::codec::Streaming<::proto_flow::shuffle::QueueResponse>>,
            tonic::Status,
        > {
            self.inner.ready().await.map_err(|e| {
                tonic::Status::unknown(format!("Service was not ready: {}", e.into()))
            })?;
            let codec = tonic_prost::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static("/shuffle.Shuffle/Queue");
            let mut req = request.into_streaming_request();
            req.extensions_mut()
                .insert(GrpcMethod::new("shuffle.Shuffle", "Queue"));
            self.inner.streaming(req, path, codec).await
        }
    }
}
/// Generated server implementations.
#[cfg(feature = "shuffle_server")]
pub mod shuffle_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with ShuffleServer.
    #[async_trait]
    pub trait Shuffle: std::marker::Send + std::marker::Sync + 'static {
        /// Server streaming response type for the Session method.
        type SessionStream: tonic::codegen::tokio_stream::Stream<
                Item = std::result::Result<::proto_flow::shuffle::SessionResponse, tonic::Status>,
            > + std::marker::Send
            + 'static;
        /// Session is the top-level RPC opened by the Coordinator (typically
        /// shard-000). It manages the shuffle lifecycle, journal watches,
        /// and checkpoint aggregation.
        async fn session(
            &self,
            request: tonic::Request<tonic::Streaming<::proto_flow::shuffle::SessionRequest>>,
        ) -> std::result::Result<tonic::Response<Self::SessionStream>, tonic::Status>;
        /// Server streaming response type for the Slice method.
        type SliceStream: tonic::codegen::tokio_stream::Stream<
                Item = std::result::Result<::proto_flow::shuffle::SliceResponse, tonic::Status>,
            > + std::marker::Send
            + 'static;
        /// Slice is opened by the Session to each member. Each Slice reads
        /// an assigned subset of journals and routes documents to Queues.
        async fn slice(
            &self,
            request: tonic::Request<tonic::Streaming<::proto_flow::shuffle::SliceRequest>>,
        ) -> std::result::Result<tonic::Response<Self::SliceStream>, tonic::Status>;
        /// Server streaming response type for the Queue method.
        type QueueStream: tonic::codegen::tokio_stream::Stream<
                Item = std::result::Result<::proto_flow::shuffle::QueueResponse, tonic::Status>,
            > + std::marker::Send
            + 'static;
        /// Queue is opened by each Slice to each member. Documents flow
        /// from Slices through Queues to on-disk queue files.
        async fn queue(
            &self,
            request: tonic::Request<tonic::Streaming<::proto_flow::shuffle::QueueRequest>>,
        ) -> std::result::Result<tonic::Response<Self::QueueStream>, tonic::Status>;
    }
    /// Shuffle service provides coordinated, disk-backed shuffled reads.
    #[derive(Debug)]
    pub struct ShuffleServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> ShuffleServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(inner: T, interceptor: F) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>> for ShuffleServer<T>
    where
        T: Shuffle,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/shuffle.Shuffle/Session" => {
                    #[allow(non_camel_case_types)]
                    struct SessionSvc<T: Shuffle>(pub Arc<T>);
                    impl<T: Shuffle>
                        tonic::server::StreamingService<::proto_flow::shuffle::SessionRequest>
                        for SessionSvc<T>
                    {
                        type Response = ::proto_flow::shuffle::SessionResponse;
                        type ResponseStream = T::SessionStream;
                        type Future =
                            BoxFuture<tonic::Response<Self::ResponseStream>, tonic::Status>;
                        fn call(
                            &mut self,
                            request: tonic::Request<
                                tonic::Streaming<::proto_flow::shuffle::SessionRequest>,
                            >,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move { <T as Shuffle>::session(&inner, request).await };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = SessionSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.streaming(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/shuffle.Shuffle/Slice" => {
                    #[allow(non_camel_case_types)]
                    struct SliceSvc<T: Shuffle>(pub Arc<T>);
                    impl<T: Shuffle>
                        tonic::server::StreamingService<::proto_flow::shuffle::SliceRequest>
                        for SliceSvc<T>
                    {
                        type Response = ::proto_flow::shuffle::SliceResponse;
                        type ResponseStream = T::SliceStream;
                        type Future =
                            BoxFuture<tonic::Response<Self::ResponseStream>, tonic::Status>;
                        fn call(
                            &mut self,
                            request: tonic::Request<
                                tonic::Streaming<::proto_flow::shuffle::SliceRequest>,
                            >,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move { <T as Shuffle>::slice(&inner, request).await };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = SliceSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.streaming(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/shuffle.Shuffle/Queue" => {
                    #[allow(non_camel_case_types)]
                    struct QueueSvc<T: Shuffle>(pub Arc<T>);
                    impl<T: Shuffle>
                        tonic::server::StreamingService<::proto_flow::shuffle::QueueRequest>
                        for QueueSvc<T>
                    {
                        type Response = ::proto_flow::shuffle::QueueResponse;
                        type ResponseStream = T::QueueStream;
                        type Future =
                            BoxFuture<tonic::Response<Self::ResponseStream>, tonic::Status>;
                        fn call(
                            &mut self,
                            request: tonic::Request<
                                tonic::Streaming<::proto_flow::shuffle::QueueRequest>,
                            >,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move { <T as Shuffle>::queue(&inner, request).await };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = QueueSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.streaming(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => Box::pin(async move {
                    let mut response = http::Response::new(tonic::body::Body::default());
                    let headers = response.headers_mut();
                    headers.insert(
                        tonic::Status::GRPC_STATUS,
                        (tonic::Code::Unimplemented as i32).into(),
                    );
                    headers.insert(
                        http::header::CONTENT_TYPE,
                        tonic::metadata::GRPC_CONTENT_TYPE,
                    );
                    Ok(response)
                }),
            }
        }
    }
    impl<T> Clone for ShuffleServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "shuffle.Shuffle";
    impl<T> tonic::server::NamedService for ShuffleServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
