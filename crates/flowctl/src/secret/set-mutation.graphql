mutation SetSecret($catalogName: Name!, $document: SecretDocument!) {
    setSecret(catalogName: $catalogName, document: $document) {
        changed
        secret {
            catalogName
            secretId
        }
    }
}
