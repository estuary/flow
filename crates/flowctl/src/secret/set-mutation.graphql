mutation SetSecret($catalogName: Name!, $document: JSON!) {
    setSecret(catalogName: $catalogName, document: $document) {
        changed
        secret {
            catalogName
            secretId
        }
    }
}
