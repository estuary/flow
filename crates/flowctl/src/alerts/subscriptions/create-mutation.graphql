mutation CreateAlertSubscription(
    $prefix: Prefix!
    $email: String!
    $alertTypes: [AlertType!]
    $detail: String
) {
    createAlertSubscription(
        prefix: $prefix
        email: $email
        alertTypes: $alertTypes
        detail: $detail
    ) {
        alertTypes
    }
}
