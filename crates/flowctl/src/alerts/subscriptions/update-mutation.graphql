mutation UpdateAlertSubscription(
    $prefix: Prefix!
    $email: String!
    $alertTypes: [AlertType!]
    $detail: String
) {
    updateAlertSubscription(
        prefix: $prefix
        email: $email
        alertTypes: $alertTypes
        detail: $detail
    ) {
        alertTypes
        detail
    }
}
