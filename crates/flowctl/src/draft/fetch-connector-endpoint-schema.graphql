query FetchConnectorEndpointSchema($imageName: String!, $imageTag: String!) {
    connector(imageName: $imageName) {
        connectorTag(imageTag: $imageTag, orDefault: true) {
            imageTag
            endpointSpecSchema
        }
    }
}
