load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "proto-build",
    srcs = glob(["src/**/*.rs"]),
    crate_root = "src/lib.rs",
    edition = "2024",
    visibility = ["//visibility:public"],
)

rust_test(
    name = "proto-build_lib_test",
    crate = ":proto-build",
    edition = "2024",
)
