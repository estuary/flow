// This file is @generated by prost-build.
/// Request is one message of a Connector RPC's request stream.
///
/// The FIRST request MUST set `start` AND exactly one protocol request, which
/// determines the connector type for the life of the stream. Every later
/// request sets exactly one protocol request of that same type, and MUST NOT
/// set `start`. Only the first request is inspected: its endpoint
/// configuration is extracted and unsealed, and its task identity is
/// authorized against the bearer's claims. All later requests pass through.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Request {
    #[prost(message, optional, tag = "1")]
    pub start: ::core::option::Option<request::Start>,
    #[prost(oneof = "request::Kind", tags = "2, 3, 4")]
    pub kind: ::core::option::Option<request::Kind>,
}
/// Nested message and enum types in `Request`.
pub mod request {
    #[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
    pub struct Start {
        /// Log level of the connector.
        #[prost(enumeration = "super::super::ops::log::Level", tag = "1")]
        pub log_level: i32,
        /// URL of a SQLite VFS which a derive-sqlite connector should open.
        /// Runtime-internal: set only by an in-process shard hosting a recorded
        /// recovery log. Empty for all other connectors; setting it for any
        /// connector type but Sqlite is an error.
        #[prost(string, tag = "2")]
        pub sqlite_vfs_uri: ::prost::alloc::string::String,
    }
    #[derive(Clone, PartialEq, ::prost::Oneof)]
    pub enum Kind {
        #[prost(message, tag = "2")]
        Capture(super::super::capture::Request),
        #[prost(message, tag = "3")]
        Derive(super::super::derive::Request),
        #[prost(message, tag = "4")]
        Materialize(super::super::materialize::Request),
    }
}
/// / Response is one message of a Connector RPC's response stream.
///
/// The stream is zero or more `log`, then `started` carrying the connector's
/// Spec, then a mix of `log` and protocol responses of the request stream's
/// type until EOF.
///
/// A failure before the connector is running ends the stream with a Status and
/// no `started`. The stream ends (EOF or Status) only after BOTH the
/// connector's protocol responses AND its stderr have been read through, so
/// a terminal Status is always the stream's last word and is preceded by
/// every log the connector emitted.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Response {
    #[prost(oneof = "response::Kind", tags = "1, 2, 3, 4, 5")]
    pub kind: ::core::option::Option<response::Kind>,
}
/// Nested message and enum types in `Response`.
pub mod response {
    #[derive(Clone, PartialEq, ::prost::Message)]
    pub struct Started {
        /// Running connector container, if the connector is an image.
        #[prost(message, optional, tag = "1")]
        pub container: ::core::option::Option<super::super::runtime::Container>,
        #[prost(enumeration = "started::Codec", tag = "2")]
        pub codec: i32,
        /// Deadline for gracefully restarting the session ahead of injected IAM
        /// credential expiry. Absent when the connector uses no IAM auth.
        #[prost(message, optional, tag = "3")]
        pub token_restart_at: ::core::option::Option<::pbjson_types::Timestamp>,
        /// Process serving this RPC: its ID and directly dial-able endpoint.
        /// Absent in local contexts.
        #[prost(message, optional, tag = "4")]
        pub process: ::core::option::Option<::proto_gazette::broker::ProcessSpec>,
        /// The connector's response to a Spec request, which the service issues as
        /// part of starting it. Always set, of the stream's protocol.
        #[prost(oneof = "started::Spec", tags = "5, 6, 7")]
        pub spec: ::core::option::Option<started::Spec>,
    }
    /// Nested message and enum types in `Started`.
    pub mod started {
        /// Wire codec negotiated with the connector.
        #[derive(
            Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration,
        )]
        #[repr(i32)]
        pub enum Codec {
            Invalid = 0,
            Proto = 1,
            Json = 2,
        }
        impl Codec {
            /// String value of the enum field names used in the ProtoBuf definition.
            ///
            /// The values are not transformed in any way and thus are considered stable
            /// (if the ProtoBuf definition does not change) and safe for programmatic use.
            pub fn as_str_name(&self) -> &'static str {
                match self {
                    Self::Invalid => "INVALID",
                    Self::Proto => "PROTO",
                    Self::Json => "JSON",
                }
            }
            /// Creates an enum from field names used in the ProtoBuf definition.
            pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
                match value {
                    "INVALID" => Some(Self::Invalid),
                    "PROTO" => Some(Self::Proto),
                    "JSON" => Some(Self::Json),
                    _ => None,
                }
            }
        }
        /// The connector's response to a Spec request, which the service issues as
        /// part of starting it. Always set, of the stream's protocol.
        #[derive(Clone, PartialEq, ::prost::Oneof)]
        pub enum Spec {
            #[prost(message, tag = "5")]
            Capture(::prost::alloc::boxed::Box<super::super::super::capture::response::Spec>),
            #[prost(message, tag = "6")]
            Derive(::prost::alloc::boxed::Box<super::super::super::derive::response::Spec>),
            #[prost(message, tag = "7")]
            Materialize(
                ::prost::alloc::boxed::Box<super::super::super::materialize::response::Spec>,
            ),
        }
    }
    #[derive(Clone, PartialEq, ::prost::Oneof)]
    pub enum Kind {
        #[prost(message, tag = "1")]
        Started(Started),
        #[prost(message, tag = "2")]
        Log(super::super::ops::Log),
        #[prost(message, tag = "3")]
        Capture(super::super::capture::Response),
        #[prost(message, tag = "4")]
        Derive(super::super::derive::Response),
        #[prost(message, tag = "5")]
        Materialize(super::super::materialize::Response),
    }
}
