test://root:
    import:
        - sibling
        - test://not/found
    nodeDependencies:
        package-one: "v0.1.2"
        pkg-2: "~v2"
    collections:
        a/collection:
            schema: "test://example/schema"
            key: [/key]
            store: { name: endpoint }
            projections:
                foo: "/bar"
                baz:
                    location: "/bing"
                    partition: true

test://root/sibling: {}
test://example/schema: true
